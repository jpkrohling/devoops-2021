extensions:

receivers:
  otlp:
    protocols:
      grpc:
      http:

processors:
  devoops:

exporters:
  jaeger:
    endpoint: localhost:14250
    tls:
      insecure: true

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [devoops]
      exporters: [jaeger]
  extensions: []
